@*@model IEnumerable<Msit115BestOne.Models.Cart>*@
@{
    ViewBag.Title = "收件人欄位";
}
<br />
<br />
<form class="form-horizontal" method="post" action="~/ShoppingCart/Recipient/Edit">
    <div class="form-group">
        <label for="Recipient" class="col-sm-2 control-label">收件人姓名：</label>
        <div class="col-sm-10">
            @*<input type="hidden" name="CartID" value="@Model.CartID" />
            <input type="hidden" name="ShoppingCartID" value="@Model.ShoppingCartID" />
            <input type="hidden" name="CaseID" value="@Model.CaseID" />
            <input type="hidden" name="MemberID" value="@Model.MemberID" />
            <input type="hidden" name="Point" value="@Model.Point" />
            <input type="hidden" name="Quantity" value="@Model.Quantity" />
            <input type="hidden" name="DateCreated" value="@Model.DateCreated" />*@
            <input type="hidden" name="ShoppingCartID" value="@Model" />            
            <input type="text" class="form-control" id="Recipient" name="Recipient" placeholder="收件人姓名" />
        </div>
    </div>
    <div class="form-group">
        <label for="RecipientPhone" class="col-sm-2 control-label">收件人電話：</label>
        <div class="col-sm-10">
            <input type="tel" class="form-control" id="RecipientPhone" name="RecipientPhone" placeholder="收件人電話">
        </div>
    </div>
    <div class="form-group">
        <label for="RecipientAddress" class="col-sm-2 control-label">收件人地址：</label>
        <div class="col-sm-10">
            <input type="text" class="form-control" id="RecipientAddress" name="RecipientAddress" placeholder="收件人地址">
        </div>
    </div>
    @*<div class="form-group">
        <div class="col-sm-offset-2 col-sm-10">
            <div class="checkbox">
                <label>
                    <input type="checkbox">同會員資料
                </label>
            </div>
        </div>
    </div>*@
    <div class="form-group">
        <div class="col-sm-offset-2 col-sm-10">
            @* @Html.ActionLink("確認訂購","Edit",null,new {@class="btn btn-default"})*@
            <button type="submit" class="btn btn-default"><span class="glyphicon glyphicon-ok"></span>確認</button>

        </div>
    </div>
</form>
<br />
<br />
